# เอกสารทดสอบ (Test Cases) — QCItem Migration Issue #22

| หัวข้อ | รายละเอียด |
|---|---|
| **เรื่องที่แก้ไข** | เพิ่มหน้า Nuxt สำหรับจัดการ QCItem ที่ `/csm-next/master/qc-item/` แทนการทำงานของหน้า legacy โดยคง backend contract เดิม |
| **ขอบเขตที่กระทบ** | อ่านรายการ, pagination, เพิ่ม/แก้ไข/ลบในตาราง, validation, บันทึกแบบแทนที่ทั้งชุด, นำเข้า `.xlsx`, ส่งออก Excel, authentication, loading/empty/error และ responsive target UI |
| **หน้าจอ / เมนู** | เปิดตรงที่ `/csm-next/master/qc-item/` หลังเข้าสู่ระบบ Mango CSM |
| **Endpoint ที่เกี่ยวข้อง** | `/CSM/Master/QCItem_ReadList`, `/CSM/Master/QCItem_Create`, `/CSM/Master/QCItem_Import`, `/CSM/Master/QCItem_ExportExcel`, ดาวน์โหลดผ่าน `/Api/File/DownLoad?download=true&id=...` |
| **วันที่จัดทำ / ปรับปรุง** | 2026-09-14 / 2026-09-15 |

## สภาพแวดล้อมและบัญชีทดสอบสำหรับ PR #23

ใช้หน้า `Nuxt/app/pages/master/qc-item/index.vue` ที่ `/csm-next/master/qc-item/` เท่านั้น เปิด DevTools → Network และเปิด Preserve log ก่อนทดสอบแต่ละบัญชี

| บัญชี | สิทธิ์ exact-match `menu_name=CSM_WEB`, `menu_id=21010` | ข้อมูลที่เตรียม |
|---|---|---|
| A: ไม่มีสิทธิ์ | ไม่มีแถวตรงกัน หรือ `isenabled=0` | session จริงที่ยังไม่หมดอายุ |
| B: อ่านอย่างเดียว | `isenabled=1`, `isreadonly=1` | QCItem อย่างน้อย 11 รายการ |
| C: แก้ไขได้ | `isenabled=1`, `isreadonly=0` | บริษัททดสอบและไฟล์ `QCItem.xlsx`, `QCItem.xls` |

| Operation | Area / Controller / Action หรือ endpoint |
|---|---|
| ตรวจสิทธิ์ | `api/public/ViewUserAuthentication?menu_name=CSM_WEB&lang_code=&menu_id=21010` |
| อ่าน | `CSM / Master / QCItem_ReadList` |
| บันทึกทั้งชุด | `CSM / Master / QCItem_Create` |
| นำเข้า | `CSM / Master / QCItem_Import` — multipart field `file`, `.xlsx` เท่านั้น |
| ส่งออก | `CSM / Master / QCItem_ExportExcel` |
| ดาวน์โหลด | `Api / File / DownLoad?download=true&id=...` |

กรณี TC-02–TC-05 และกรณีแก้ไขใน TC-06 ใช้บัญชี C เท่านั้น บัญชี B ต้องอ่านและส่งออกได้โดยไม่มีการแก้ไข

---

## รายการ Test Case

### TC-01: เปิดหน้าและแสดงรายการ QCItem พร้อม pagination

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Happy path |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | Login ด้วยบัญชีภายในที่มีสิทธิ์เข้าถึงข้อมูล QCItem และบริษัทมีข้อมูลมากกว่า 10 รายการ |
| **ข้อมูลทดสอบ (Test Data)** | รายการ QCItem อย่างน้อย 11 รายการ เช่น `itemno=1..11`, Description และ Remark ไม่ว่าง |

**ขั้นตอนการทดสอบ (Test Steps):**

- [ ] **1.** เปิด URL `/csm-next/master/qc-item/` โดยตรงและ refresh หน้า
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบตรวจสอบ session สำเร็จและแสดงหน้า `QC Item list` โดยไม่ redirect ไปหน้า login
- [ ] **2.** ตรวจสอบตารางรายการ
  - _ผลลัพธ์ที่คาดหวัง:_ ตารางมีคอลัมน์ `No.`, `Action`, `Description`, `Remark`, `Add date` และข้อมูลหน้าแรกไม่เกิน 10 แถว
- [ ] **3.** ตรวจสอบเลขลำดับแถวและกด pagination ไปหน้าที่ 2
  - _ผลลัพธ์ที่คาดหวัง:_ เลขลำดับเป็นเลขต่อเนื่องจาก 1 และหน้าที่ 2 เริ่มด้วยเลข 11 โดยรายการไม่ซ้ำกับหน้าแรก

### TC-02: เพิ่ม แก้ไข ลบ และบันทึกรายการ QCItem

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Happy path |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | อยู่หน้า QCItem และมีข้อมูลเดิมอย่างน้อย 1 รายการ |
| **ข้อมูลทดสอบ (Test Data)** | Description ใหม่ `ความสะอาดพื้นที่`, Remark ใหม่ `ตรวจทุกสิ้นวัน` |

**ขั้นตอนการทดสอบ (Test Steps):**

- [ ] **1.** กดปุ่ม `เพิ่มรายการ`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบเพิ่มแถวใหม่ท้ายชุดข้อมูลและช่อง Description/Remark ว่างเพื่อแก้ไขทันที; หลัง Save backend อาจจัด `itemno` ใหม่เป็นลำดับ 1..N ตาม legacy contract
- [ ] **2.** กรอก Description = `ความสะอาดพื้นที่` และ Remark = `ตรวจทุกสิ้นวัน` ในแถวใหม่
  - _ผลลัพธ์ที่คาดหวัง:_ ค่าที่กรอกแสดงอยู่ในช่องตารางโดยไม่ต้องเปิดหน้าแก้ไขแยก
- [ ] **3.** เลือกแถวเดิมที่มีข้อมูลแล้วกดปุ่มลบ และยืนยันการลบ
  - _ผลลัพธ์ที่คาดหวัง:_ แถวถูกนำออกจากรายการบนหน้าจอ และยังไม่มี request บันทึกจนกว่าจะกด `บันทึก`
- [ ] **4.** กดปุ่ม `บันทึก`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบบันทึกรายการทั้งชุดผ่าน `QCItem_Create`, แสดงข้อความบันทึกสำเร็จ และโหลดรายการล่าสุดกลับมา
- [ ] **5.** refresh หน้าแล้วตรวจสอบรายการ
  - _ผลลัพธ์ที่คาดหวัง:_ รายการที่เพิ่ม/แก้ไขยังอยู่ รายการที่ลบไม่กลับมา และเลข `itemno` หลัง Save เป็นลำดับที่ backend จัดใหม่ตาม legacy contract

### TC-03: ป้องกันการบันทึกเมื่อ Description หรือ Remark ว่าง

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Negative / Validation |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | อยู่หน้า QCItem และมีอย่างน้อย 1 แถว |
| **ข้อมูลทดสอบ (Test Data)** | เว้น Description ว่างในกรณีแรก และเว้น Remark หรือกรอกเป็นช่องว่างในกรณีที่สอง |

**ขั้นตอนการทดสอบ (Test Steps):**

- [ ] **1.** ลบค่าจากช่อง Description ของแถวหนึ่งให้ว่าง แล้วกด `บันทึก`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบไม่ส่ง `QCItem_Create` และแสดงข้อความแจ้งให้กรอก Description พร้อมเลขรายการที่มีปัญหา
- [ ] **2.** กรอก Description กลับมา แล้วลบค่าจากช่อง Remark หรือกรอกเฉพาะช่องว่าง จากนั้นกด `บันทึก`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบไม่ส่ง `QCItem_Create` และแสดงข้อความแจ้งให้กรอก Remark พร้อมเลขรายการที่มีปัญหา
- [ ] **3.** กรอกข้อมูลให้ครบแล้วกด `บันทึก`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบยอมให้บันทึกและปุ่มบันทึกถูก disable ระหว่าง request เพื่อป้องกันการกดซ้ำ

### TC-04: นำเข้าไฟล์ QCItem ด้วย `.xlsx`

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Happy path / Negative |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | Login สำเร็จและมีไฟล์ Excel ที่มีหัวตาราง/ข้อมูล `itemno`, `itemname`, `remark` |
| **ข้อมูลทดสอบ (Test Data)** | `QCItem.xlsx` ที่มีแถว `1, ความสะอาดพื้นที่, ตรวจทุกสิ้นวัน` และไฟล์ผิดประเภท `QCItem.pdf` |

**ขั้นตอนการทดสอบ (Test Steps):**

- [ ] **1.** กด `นำเข้า Excel` แล้วเลือกไฟล์ `QCItem.xlsx`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบเปิด dialog นำเข้าและแสดงชื่อไฟล์ที่เลือก โดยยังไม่ upload ทันที
- [ ] **2.** กด `อัปโหลด`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบส่งไฟล์ด้วย field ชื่อ `file` ไปยัง `QCItem_Import`, ปุ่มอัปโหลดถูก disable ระหว่างดำเนินการ และเมื่อสำเร็จ dialog ปิดพร้อมโหลดรายการใหม่
- [ ] **3.** เลือกไฟล์ `QCItem.pdf`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบไม่ส่ง upload และแสดงข้อความให้เลือกไฟล์ `.xlsx` เท่านั้น
- [ ] **4.** ใช้ไฟล์ `.xlsx` ที่ backend ปฏิเสธ แล้วกด `อัปโหลด`
  - _ผลลัพธ์ที่คาดหวัง:_ dialog ยังคงเปิดอยู่และแสดงข้อความ error จาก backend เพื่อให้เลือกยกเลิกหรือลองใหม่ได้

### TC-05: ส่งออก Excel และนำไฟล์กลับเข้าเป็น round trip

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Happy path / Regression |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | Login สำเร็จและมีข้อมูล QCItem; backend สามารถสร้าง export token ได้ |
| **ข้อมูลทดสอบ (Test Data)** | ข้อมูล QCItem ปัจจุบัน และไฟล์ที่ได้จากปุ่ม `ส่งออก Excel` |

**ขั้นตอนการทดสอบ (Test Steps):**

- [ ] **1.** กดปุ่ม `ส่งออก Excel`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบเรียก `QCItem_ExportExcel` แล้วเปิด download URL ผ่าน `Api/File/DownLoad?download=true&id=...` โดยใช้ token จาก server
- [ ] **2.** ตรวจสอบไฟล์ที่ดาวน์โหลด
  - _ผลลัพธ์ที่คาดหวัง:_ ไฟล์เป็น `.xlsx`, เปิดได้, มีชื่อไฟล์จาก server และข้อมูล/คอลัมน์ไม่เสียหาย
- [ ] **3.** นำไฟล์ที่ได้กลับเข้าผ่าน `นำเข้า Excel`
  - _ผลลัพธ์ที่คาดหวัง:_ backend รับไฟล์ได้และรายการบนหน้าจอถูก refresh โดยข้อมูลหลักของ QCItem ยังคงตรงกับไฟล์

### TC-06: Authentication และสถานะ loading/error/empty

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Regression |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | ทดสอบทั้งกรณีมี session, ไม่มี session, API error และบริษัทไม่มีข้อมูล |
| **ข้อมูลทดสอบ (Test Data)** | session ที่หมดอายุ/ไม่มี token, response HTTP 401, response HTTP 403, และ raw list `[]` |

**ขั้นตอนการทดสอบ (Test Steps):**

- [ ] **1.** เปิด route โดยไม่มี internal session
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบแสดงสถานะต้องเข้าสู่ระบบและ redirect ไป login โดยไม่เรียก `QCItem_ReadList`
- [ ] **2.** เปิด route ด้วย session ที่ใช้ได้ ขณะจำลอง API กำลังตอบกลับ
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบแสดง loading state และปุ่ม/การกระทำของหน้าจอไม่ทำให้เกิด request ซ้ำโดยไม่จำเป็น
- [ ] **3.** จำลอง `QCItem_ReadList` ตอบ HTTP 401 แล้วเปิดหน้าใหม่
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบจัดการเป็น session หมดอายุและไม่แสดงเป็น empty list
- [ ] **4.** จำลอง `QCItem_ReadList` ตอบ HTTP 403
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบแสดง error/permission state โดยไม่ล้าง session อัตโนมัติ
- [ ] **5.** จำลอง `QCItem_ReadList` ตอบ raw array ว่าง `[]`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบแสดง empty state พร้อมปุ่ม `เพิ่มรายการ` ให้เริ่มสร้างข้อมูลได้
- [ ] **6.** จำลอง `QCItem_ReadList` ตอบ HTTP 200 แต่ body ไม่ใช่ raw array เช่น `{}` แล้วตรวจสอบปุ่ม `บันทึก`
  - _ผลลัพธ์ที่คาดหวัง:_ ระบบแสดง invalid-response/error state ไม่แสดงเป็น empty list และปุ่ม `บันทึก` ถูก disable เพื่อป้องกันการส่งชุดข้อมูลว่างไปแทนที่ข้อมูลเดิม
- [ ] **7.** เริ่ม request `บันทึก` หรือ `อัปโหลด` แล้วลองแก้ไข/เพิ่ม/ลบรายการระหว่างรอผล
  - _ผลลัพธ์ที่คาดหวัง:_ controls สำหรับแก้ไขชุดข้อมูลถูก disable จนกว่า request และ reload จะเสร็จ เพื่อไม่ให้ local edits ถูกทับโดย response เก่า
- [ ] **8.** ทดสอบหน้าจอที่ความกว้างประมาณ 390px
  - _ผลลัพธ์ที่คาดหวัง:_ action buttons จัดเรียงอ่านได้ ตารางเลื่อนได้ในแนวนอน และไม่มีข้อมูล/ปุ่มถูกตัดจนใช้งานไม่ได้

---

### TC-07: บัญชีไม่มีสิทธิ์ถูกปฏิเสธก่อนโหลดข้อมูล

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Authorization / Negative |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | Login ด้วยบัญชี A และล้าง Network log |
| **ข้อมูลทดสอบ (Test Data)** | ไม่มีแถว `CSM_WEB/21010` ที่เปิดใช้งาน |

- [ ] **1.** เปิด route โดยตรงแล้ว refresh ที่ desktop และ 390 px
  - _ผลลัพธ์ที่คาดหวัง:_ ตรวจสิทธิ์ก่อน แสดงข้อความไม่มีสิทธิ์ ไม่มีตารางหรือ controls สำหรับอ่าน/แก้ไข/ส่งออก
- [ ] **2.** กรอง Network ด้วย `QCItem_`
  - _ผลลัพธ์ที่คาดหวัง:_ จำนวน request ReadList, Create, Import และ ExportExcel เท่ากับ 0 ทั้งสองขนาดหน้าจอ

### TC-08: บัญชีอ่านอย่างเดียวอ่าน เปลี่ยนหน้า และส่งออกได้

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Authorization / Happy path |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | Login ด้วยบัญชี B และมี QCItem อย่างน้อย 11 รายการ |
| **ข้อมูลทดสอบ (Test Data)** | แถวสิทธิ์เดียว `isenabled=1`, `isreadonly=1` |

- [ ] **1.** เปิด route และตรวจ Network
  - _ผลลัพธ์ที่คาดหวัง:_ ViewUserAuthentication เสร็จก่อน ReadList และแสดงป้ายอ่านอย่างเดียว
- [ ] **2.** ไปหน้าที่ 2 แล้วกลับหน้าแรก
  - _ผลลัพธ์ที่คาดหวัง:_ pagination ทำงาน ข้อมูลครบและไม่ซ้ำ
- [ ] **3.** ลองแก้ Description/Remark และกดเพิ่ม ลบ บันทึก นำเข้า
  - _ผลลัพธ์ที่คาดหวัง:_ inputs และปุ่มดังกล่าว disabled; ไม่เปิด dialog/ตัวเลือกไฟล์ และไม่มี Create/Import request
- [ ] **4.** กดส่งออกแล้วตรวจชื่อไฟล์และเปิด workbook
  - _ผลลัพธ์ที่คาดหวัง:_ ExportExcel และ download ทำงาน ไฟล์ `.xlsx` เปิดได้และข้อมูลตรงรายการ
- [ ] **5.** ทำข้อ 1–4 ซ้ำที่ 390 px พร้อมตรวจทุก permission control รวมช่องเลือกไฟล์และอัปโหลดหากมี dialog ค้างจากการเปลี่ยนสิทธิ์
  - _ผลลัพธ์ที่คาดหวัง:_ สิทธิ์เหมือน desktop ไม่มีช่องทางแก้ไขหรือ Import ที่ใช้งานได้

### TC-09: บัญชีแก้ไขได้คงพฤติกรรมเดิมทุกเส้นทาง

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Authorization / Regression |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | Login ด้วยบัญชี C ในบริษัททดสอบ |
| **ข้อมูลทดสอบ (Test Data)** | Description `ความสะอาดพื้นที่`, Remark `ตรวจทุกสิ้นวัน`, `QCItem.xlsx` |

- [ ] **1.** ทำ TC-01–TC-05 รวม refresh หลัง Save และ Import
  - _ผลลัพธ์ที่คาดหวัง:_ อ่าน เพิ่ม แก้ไข ลบ บันทึก นำเข้า ส่งออก และ persistence ผ่านกับ backend จริง
- [ ] **2.** ทำขั้นตอนสำคัญซ้ำที่ 390 px ตรวจ Add/Delete/Save/Import, inputs, file picker, Upload, Cancel, Export และ pagination
  - _ผลลัพธ์ที่คาดหวัง:_ controls เข้าถึงได้ ไม่ถูกตัด และ disabled ระหว่างงานตามนโยบายเดิม; Cancel ปิดไม่ได้ขณะ upload ค้าง

### TC-10: Add Date มีวินาทีและคงเวลาในค่า ISO-like

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Display / Regression |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | บัญชี B หรือ C และ backend มีค่า Add Date ที่ทราบค่าต้นฉบับ |
| **ข้อมูลทดสอบ (Test Data)** | `2026-09-14T10:05:06Z`, `2026-09-14T10:05:06.123+07:00`, `2026-09-14 23:59:58`, `2024-02-29`, `2026-13-14`, `2026-02-29`, `2026-09-14T24:00:00Z`, วันที่ว่าง/ผิดรูปแบบ |

- [ ] **1.** เทียบ Add Date บนตารางกับ response ที่ desktop และ 390 px
  - _ผลลัพธ์ที่คาดหวัง:_ `2026-09-14T10:05:06Z` และ `2026-09-14T10:05:06.123+07:00` แสดง `14/09/2026 10:05:06`; `2026-09-14 23:59:58` แสดง `14/09/2026 23:59:58`; ไม่มี timezone shift; ค่าว่าง/ผิดรูปแบบเป็นข้อความว่าง
- [ ] **2.** ตรวจวันที่แบบ date-only และ .NET date ที่มีในข้อมูลจริง
  - _ผลลัพธ์ที่คาดหวัง:_ date-only แสดง `00:00:00`; .NET date แสดงเวลาท้องถิ่นพร้อมวินาที
- [ ] **3.** ใช้ข้อมูล QA ที่มี leap date และวันที่/เวลาเกินช่วง แล้วเทียบกับ response ต้นฉบับ
  - _ผลลัพธ์ที่คาดหวัง:_ วันที่ 29 กุมภาพันธ์ 2024 แสดงได้; เดือน/วัน/leap date/ชั่วโมง/นาที/วินาทีที่ผิดแสดงว่าง เช่น `2026/02/29`; .NET wrapper ผิด เช่น `/Date(0)/junk` แสดงว่าง; timestamp ที่มี offset คงชั่วโมงเดิม

### TC-11: ปฏิเสธ `.xls` ก่อนส่ง Upload

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Import / Negative |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | บัญชี C และเปิด dialog นำเข้า |
| **ข้อมูลทดสอบ (Test Data)** | `QCItem.xls`, `QCItem.xlsx`, `QCItem.XLSX` |

- [ ] **1.** เลือก `QCItem.xls` ผ่านตัวเลือกไฟล์ (เลือก All files หากจำเป็น)
  - _ผลลัพธ์ที่คาดหวัง:_ แสดงข้อความให้เลือก `.xlsx` เท่านั้น และ Upload ไม่พร้อมใช้งาน
- [ ] **2.** ตรวจ Network และลองกด Upload
  - _ผลลัพธ์ที่คาดหวัง:_ ไม่มี request `QCItem_Import` แม้พยายามอัปโหลดไฟล์ที่ถูกปฏิเสธ
- [ ] **3.** เปลี่ยนเป็น `.xlsx` และ `.XLSX` ที่ถูกต้อง แล้วทำซ้ำที่ desktop และ 390 px
  - _ผลลัพธ์ที่คาดหวัง:_ ทั้งสองนามสกุลเลือกได้และส่งได้ตาม TC-04

### TC-12: แยก access error/data error และไม่คืนข้อมูลจาก request เก่า

| รายการ | รายละเอียด |
|---|---|
| **ประเภท** | Authorization / Retry / Regression |
| **เงื่อนไขเริ่มต้น (Pre-condition)** | สภาพแวดล้อม QA ที่ควบคุมการตอบกลับได้; แยกหลักฐานจำลองออกจาก real-account UAT |
| **ข้อมูลทดสอบ (Test Data)** | auth หมดอายุ, auth response ผิดรูปแบบ/สิทธิ์ซ้ำ, network error, ReadList ล่าช้า |

- [ ] **1.** เปิด route โดยไม่มี session และด้วย session ที่หมดอายุ
  - _ผลลัพธ์ที่คาดหวัง:_ ไป login; local anonymous ไม่เรียก Access API; access anonymous ไม่เรียก QCItem endpoints
- [ ] **2.** ทำให้ Access API ล้มเหลว แล้วกดลองตรวจสิทธิ์อีกครั้ง
  - _ผลลัพธ์ที่คาดหวัง:_ แสดง access error แยกจาก list error; ก่อนสำเร็จไม่มี QCItem calls; retry เรียก Access API ใหม่
- [ ] **3.** หลังได้รับสิทธิ์ ให้ ReadList ล้มเหลวแล้วกดลองโหลดข้อมูลอีกครั้ง
  - _ผลลัพธ์ที่คาดหวัง:_ retry เฉพาะ ReadList ไม่เรียก Access API ซ้ำ
- [ ] **4.** เริ่มโหลดข้อมูลด้วยสิทธิ์ editable แล้วตรวจสิทธิ์รุ่นใหม่เป็น denied ก่อนปล่อย ReadList เก่า
  - _ผลลัพธ์ที่คาดหวัง:_ ไม่คืนตารางเก่า; dialog ปิดและไฟล์/ข้อความนำเข้าถูกล้าง; response สิทธิ์เก่าไม่ทับสิทธิ์ล่าสุด
- [ ] **5.** ตรวจข้อความ Thai/English และทุก permission control ที่ desktop และ 390 px
  - _ผลลัพธ์ที่คาดหวัง:_ checking/anonymous/denied/error/readonly อ่านได้และแสดงสิทธิ์สอดคล้องกัน
- [ ] **6.** ในสภาพแวดล้อม QA ให้ Save/Upload/Export รอผล แล้วตรวจสิทธิ์รุ่นใหม่ก่อนปล่อยผลของงานเก่า
  - _ผลลัพธ์ที่คาดหวัง:_ ผลงานเก่าไม่คืน dialog/feedback หรือเริ่ม reload ในรุ่นใหม่; popup ส่งออกเก่าปิดเมื่อสิทธิ์หมด; งานรุ่นใหม่ไม่ติด loading จากงานเก่า การตรวจนี้ไม่ยืนยันการ rollback ของ request ที่ backend รับไปแล้ว

## หลักฐานและสถานะ UAT

Automated frontend verification วันที่ 2026-09-15 ที่ code commit `67b5036`: `npm test -- --reporter=dot` ผ่าน 16 files / 177 tests; `npm audit` พบ 0 vulnerabilities; `npm run build` และ `npm run generate` exit 0; generated route `.output/public/master/qc-item/index.html` มีจริง Build/generate มี warnings ของ dependencies Nuxt/Nitro เดิม ผลนี้ไม่ใช่ real-account UAT

| สิ่งที่ต้องยืนยันด้วย backend และบัญชีจริง | Desktop | 390 px |
|---|---|---|
| A denied และ zero QCItem calls | NOT VERIFIED | NOT VERIFIED |
| B readonly: list/pagination/export และ zero mutation calls | NOT VERIFIED | NOT VERIFIED |
| C editable: read/add/edit/delete/save/import/export | NOT VERIFIED | NOT VERIFIED |
| Add Date seconds, `.xlsx`/`.xls` import, Thai/English และ retry | PARTIAL: `.xlsx` และ BIFF `.xls` import ผ่าน; รายการอื่นยังไม่ครบ | NOT VERIFIED |
| Export filename/bytes และ persistence หลัง reload | PASS: HTTP 200, workbook เปิดอ่านได้ และข้อมูลคงอยู่หลัง reload | NOT VERIFIED |
| Cross-company import isolation | Backend follow-up; not exercised by this frontend UAT | Backend follow-up; not exercised by this frontend UAT |

ยังไม่มีหลักฐานจากบัญชีจริงครบสามสิทธิ์ ต้องแนบ environment, เวลา, บัญชี/บริษัทแบบไม่เปิดเผยข้อมูลลับ, Network evidence และผู้ทดสอบเมื่อรันจริง พร้อม owner sign-off ก่อนอนุมัติ release; unit/component tests ไม่แทนหลักฐานเหล่านี้

Desktop UAT เพิ่มเติมวันที่ 2026-09-15 ใช้ backend .NET 8 และ SQL Server clone บริษัท `MGM`: บัญชี full-menu ผ่าน Login/access, Read baseline, Add/Edit/Save/refresh, Import `.xlsx`, Importไฟล์ Excel 97-2003 BIFF `.xls` จริง และ Export `.xlsx` สำเร็จ โดยไม่บันทึก credential/token ลงเอกสาร การลบผ่าน browser และการคืน baseline หลัง `.xls` import ยังรอการยืนยัน destructive action จึงยังไม่ถือว่า editable matrix ผ่านครบชุด

## หมายเหตุสำหรับ QA/UAT

- การทดสอบนี้เป็น frontend target route ที่ path `Nuxt/app/pages/master/qc-item/index.vue`; route legacy `/page/Master/v_csm_mas_011/` ไม่ได้ถูกลบหรือเปลี่ยนแปลง
- ต้องทดสอบกับ backend จริงเพื่อยืนยันสิทธิ์บริษัท, persistence, ชื่อไฟล์/bytes ของ export และพฤติกรรม import ภายในบริษัททดสอบ
- Backend target แก้ `QCItem_Import` ให้ lookup ด้วย `maincode + itemno` แล้ว; ต้องมี direct-request regression test และ real-environment evidence ก่อนปิดความเสี่ยงข้ามบริษัท
- **ข้อจำกัดด้าน security:** backend target บังคับเมนู `CSM_WEB/21010` แล้วสำหรับ ReadList/Export และบังคับ enabled + ไม่ readonly สำหรับ Create/Import; ต้องยืนยัน denied/readonly ด้วยบัญชีจริง
- **ข้อจำกัดด้าน compatibility:** backend target ใช้ NPOI รองรับทั้ง `.xlsx` และ `.xls` แบบ Excel 97-2003 BIFF แล้ว พร้อม focused parser tests; ยังต้องเพิ่มไฟล์ตัวอย่างจาก Microsoft Excel หลายรูปแบบและ integration tests ก่อน production sign-off
